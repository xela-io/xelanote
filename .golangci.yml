run:
  timeout: 5m
  tests: true
  skip-dirs:
    - vendor
    - testdata

linters:
  disable-all: true
  enable:
    - govet
    - errcheck
    - staticcheck
    - ineffassign
    - gosimple
    - typecheck

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck

